# Copy this file to config.env (locally) or set the following variables in your deployment platform (Render, Railway, etc.)
# Do NOT commit real secrets to the repository.

NODE_ENV=development
PORT=8000

# Replace with a secure, random string (do not commit this value)
JWT_SECRET=REPLACE_WITH_A_STRONG_SECRET
JWT_EXPIRES_IN=30d

# Example MongoDB connection string. Put <PASSWORD> placeholder here and set DATABASE_PASSWORD separately
DATABASE=mongodb+srv://<DB_USER>:<PASSWORD>@your-cluster.mongodb.net/tazaBazar?retryWrites=true&w=majority
DATABASE_PASSWORD=REPLACE_WITH_DB_PASSWORD

# Optional: comma-separated allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.example.com
